fastapi==0.111.0
uvicorn==0.30.1
httpx==0.27.0
pydantic==2.13.4
pydantic-settings==2.2.1
redis==5.0.4

# Optional but recommended: the event loop and HTTP parser used by the
# --loop uvloop --http httptools flags in the run command.
uvloop==0.22.1
httptools==0.8.0
